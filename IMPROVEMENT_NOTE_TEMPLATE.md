# Yeem01 Workflow Core Improvement Note

Submit only a minimal, redacted observation. Do not attach raw transcripts.

```text
role or audience class:
command and intended outcome:
friction observed:
recurrence count:
observable effect:
client, personal and credential data removed: yes|no
suggested rule, if any:
```

Use it in a task like this:

```text
-improve using this redacted note: <paste the completed fields>
```

The result should be a proposal, not an automatic source edit. A maintainer
reviews supported proposals, changes the Yeem01 source, adds a regression
fixture, generates a new version, and runs QA.
